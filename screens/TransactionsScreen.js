import React from "react";
import {StyleSheet, View, Text} from "react-native";

const TransactionsScreen = props => {
	return(
		<View>
			<Text>Place to change the settings here</Text>
		</View>
	);
}

export default TransactionsScreen;